<Window x:Class="CMBuyerStudio.Desktop.Views.ErrorDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Unexpected error"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        MinWidth="520"
        MaxWidth="760"
        MaxHeight="760"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        KeyDown="OnWindowKeyDown"
        SnapsToDevicePixels="True">

    <Border Style="{StaticResource DialogPanelStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#111724"
                    BorderBrush="{StaticResource BrushBorder}"
                    BorderThickness="0,0,0,1"
                    Padding="14,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock x:Name="TitleTextBlock"
                               VerticalAlignment="Center"
                               Foreground="{StaticResource BrushTextPrimary}"
                               FontSize="16"
                               FontWeight="SemiBold"/>

                    <Button Grid.Column="1"
                            Width="28"
                            Height="28"
                            Content="&#xE8BB;"
                            FontFamily="Segoe MDL2 Assets"
                            FontSize="10"
                            Style="{StaticResource TitleBarCloseButtonStyle}"
                            Click="OnCloseButtonClick"/>
                </Grid>
            </Border>

            <TextBlock x:Name="SummaryTextBlock"
                       Grid.Row="1"
                       Margin="20,18,20,4"
                       Foreground="{StaticResource BrushTextSecondary}"
                       FontSize="14"
                       TextWrapping="Wrap"/>

            <TextBlock x:Name="LogPathTextBlock"
                       Grid.Row="2"
                       Margin="20,0,20,0"
                       Foreground="{StaticResource BrushTextSecondary}"
                       FontSize="12"
                       TextWrapping="Wrap"/>

            <Border x:Name="DetailsHost"
                    Grid.Row="3"
                    Margin="20,14,20,0"
                    Padding="10"
                    CornerRadius="8"
                    Background="#101520"
                    BorderBrush="{StaticResource BrushBorder}"
                    BorderThickness="1"
                    Visibility="Collapsed">
                <TextBox x:Name="DetailsTextBox"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource BrushTextPrimary}"
                         FontFamily="Consolas"
                         FontSize="12"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         HorizontalScrollBarVisibility="Auto"
                         VerticalScrollBarVisibility="Auto"
                         MinHeight="100"
                         MaxHeight="280"/>
            </Border>

            <StackPanel Grid.Row="4"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="20,16,20,20">
                <Button x:Name="CopyDetailsButton"
                        Content="Copy details"
                        Width="120"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnCopyDetailsButtonClick"/>

                <Button x:Name="ToggleDetailsButton"
                        Content="Show details"
                        Width="120"
                        Margin="10,0,0,0"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Click="OnToggleDetailsButtonClick"/>

                <Button x:Name="CloseActionButton"
                        Content="Close"
                        Width="160"
                        Margin="12,0,0,0"
                        IsDefault="True"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Click="OnCloseButtonClick"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
